package com.application.mediquanta.reply;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class ReplyTest {

	/*
	@Autowired
	private ReplyService replyService;
	
	@Order(1)
	@DisplayName("댓글 작성")
	@Test
	public void createReply() {
		ReplyDTO replyDTO = new ReplyDTO();
		replyDTO.setNickname("test2");
		replyDTO.setContent("테스트입니다");
		replyDTO.setCreatedAt(new Date());
		replyDTO.setPostId(Long.valueOf(1));
		replyService.createReply(replyDTO, "공지사항");
	}
	
	@Order(2)
	@DisplayName("전체 댓글 조회")
	@Test
	public void getRepliesByPostId() {
		System.out.println(replyService.getRepliesByPostId(1));
	}
	
	@Order(3)
	@DisplayName("커뮤니티 회원 수 조회")
	@Test
	public void getReplyMemberCount() {
		System.out.println(replyService.getReplyMemberCount("공지사항"));
	}
	
	@Order(4)
	@DisplayName("댓글 수정")
	@Test
	public void updateReply() {
		ReplyDTO replyDTO = replyService.getRepliesByPostId(1).get(0);
		replyDTO.setContent("수정된 테스트입니다.");
		replyService.updateReply(replyDTO);
		System.out.println(replyService.getRepliesByPostId(1));
	}
	
	@Order(5)
	@DisplayName("댓글 삭제")
	@Test
	public void deleteReply() {
		replyService.deleteReply(1, 1);
		System.out.println(replyService.getRepliesByPostId(1));
	}
	*/
	
}
